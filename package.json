{
  "name": "beauty-kurs-seo",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-ausbildung-content.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:city": "tsx --tsconfig scripts/tsconfig.json scripts/generate-city.ts",
    "generate:all": "tsx --tsconfig scripts/tsconfig.json scripts/generate-all.ts",
    "generate:longtail": "tsx --tsconfig scripts/tsconfig.json scripts/generate-longtail.ts",
    "validate:sitemap": "tsx --tsconfig scripts/tsconfig.json scripts/sitemap-validator.ts",
    "postbuild": "node scripts/link-graph.mjs && node scripts/coverage-report.mjs && node scripts/dashboard.mjs",
    "link-graph": "node scripts/link-graph.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.98.0",
    "@astrojs/vercel": "^10.0.7",
    "@fontsource/cormorant-garamond": "^5.2.11",
    "@fontsource/inter": "^5.2.8",
    "@supabase/supabase-js": "^2.106.1",
    "astro": "^6.3.6",
    "resend": "^6.12.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "dotenv": "^17.4.2",
    "tsx": "^4.22.3"
  }
}
